services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: asoc
      POSTGRES_PASSWORD: asoc
      POSTGRES_DB: asoc
    ports:
      - "5432:5432"
    volumes:
      - asoc_pg:/var/lib/postgresql/data

volumes:
  asoc_pg:
